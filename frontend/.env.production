# Backend base URL baked into the production bundle at build time.
# Replace with your real backend URL (no trailing slash). Used by `npm run build`.
# Leave the Vite dev proxy in vite.config.ts untouched — dev still uses relative /api.
VITE_API_BASE_URL=https://api.your-domain.com
